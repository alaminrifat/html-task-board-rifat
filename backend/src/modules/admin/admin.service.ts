import { Injectable } from '@nestjs/common';

@Injectable()
export class AdminService {
    // TODO: Add admin-specific business logic (dashboard stats, user management, system settings, export, etc.)
}
